# 项目基本配置
PROJECT_NAME="Crypto Ticker Query"
VERSION="1.0.0"
DESCRIPTION="加密货币价格查询服务"
API_V1_STR="/api/v1"

# 安全配置
SECRET_KEY="your-secret-key-here"  # 建议使用随机生成的密钥
ACCESS_TOKEN_EXPIRE_MINUTES=60

# 数据库配置
DATABASE_URL="sqlite:///./sql_app.db"  # 示例使用SQLite，可替换为其他数据库URL

# CORS配置
ALLOWED_ORIGINS=["*"]

# 代理设置（如果不需要代理，可以留空）
HTTP_PROXY=""
HTTPS_PROXY=""

# Google服务配置
GOOGLE_CACHE_EXPIRE_MINUTES=5  # Google价格缓存过期时间（分钟）

# 数据目录配置
DATA_DIR="data"
POWER_CONFIG_FILE="g-power.json"

TG_BOT_TOKEN=dsdsz2123
TG_GID = 123
# 定时任务配置
PRICE_BROADCAST_INTERVAL=5  # 价格广播间隔（分钟）